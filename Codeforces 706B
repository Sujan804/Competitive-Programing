#include<bits/stdc++.h>
using namespace std;
#define kaj_ses return 0;
typedef long long ll;

int main()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    std::cout.tie(0);


    int n,i,m,j,cnt=0;
    cin>>n;
    int arr[n];
    for(i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    sort(arr,arr+n);
    cin>>m;
   while(m--)
   {
       cin>>j;
      int ans = upper_bound(arr,arr+n,j)-arr;
      cout<<ans<<endl;

   }
   kaj_ses;
   //for(i=0;i<q;i++){
      // cin>>x;
     //  int ans=upper_bound(a,a+n,x)-a;
      // cout<<ans<<endl;

      }



